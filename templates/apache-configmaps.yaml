apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-apache-page-1
data:
  index.html: |
    <html>
      <body>
        <h1>{{ .Values.apache1.message }}</h1>
        <p>Deze pagina wordt geserveerd door apache-deployment-1.</p>
      </body>
    </html>
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-apache-page-2
data:
  index.html: |
    <html>
      <body>
        <h1>{{ .Values.apache2.message }}</h1>
        <p>Deze pagina wordt geserveerd door apache-deployment-2.</p>
      </body>
    </html>
